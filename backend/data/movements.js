// Histórico de movimentações do estoque (em memória)
// Cada item: { id, type, productId?, productName, quantityBefore?, quantityAfter?, nameBefore?, nameAfter?, createdAt }
let movements = [];

module.exports = movements;
